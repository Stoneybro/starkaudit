# dependencies
node_modules/
.pnpm-store/

# build outputs
.next/
out/
dist/
build/

# logs
*.log

# env
.env*
!.env.example

# sealed threshold package (decrypted business secret material — never commit)
threshold-package.json

# os / editor
.DS_Store
Thumbs.db
.idea/

# typescript / next
*.tsbuildinfo
next-env.d.ts

# starknet / scarb / foundry
contracts/target/
contracts/.snfoundry_cache/

# --- secrets & account artifacts (never commit) ---
# starkli account files can contain private keys depending on how they're
# exported; only the public variant was ever committed, and it's removed.
starkli-account.json
*.account.json
accounts/
keystore.json
# retro demo notes that once contained a viewing key
report.md
